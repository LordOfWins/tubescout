{
  "name": "tubescout-extension",
  "version": "0.1.0",
  "private": true,
  "description": "TubeScout - AI YouTube Analyzer Chrome Extension",
  "scripts": {
    "dev": "webpack --mode development --watch",
    "build": "webpack --mode production",
    "watch": "webpack --mode development --watch",
    "clean": "rm -rf dist",
    "zip": "npm run build && cd dist && zip -r ../tubescout-v$npm_package_version.zip ."
  },
  "devDependencies": {
    "webpack": "^5.97.0",
    "webpack-cli": "^6.0.0",
    "css-loader": "^7.1.0",
    "mini-css-extract-plugin": "^2.9.0",
    "copy-webpack-plugin": "^12.0.0",
    "terser-webpack-plugin": "^5.3.0"
  }
}
